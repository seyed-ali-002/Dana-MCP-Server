# Dana Desktop

Flutter-based cross-platform desktop control center for Dana. The Python core remains independent; the desktop UI is a separate Flutter application.

Run from the repository root:

```bash
python3 run_gui.py
```

This requires the Flutter SDK and the relevant desktop target enabled.
